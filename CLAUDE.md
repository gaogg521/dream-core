@AGENTS.md

## ⚠️ 关键排障文档（后端 / aionrs 相关，新 AI 首读）

> **三仓 fork 对上游的映射、版本对照、当前同步状态、同步套路与不变量见：**[`../1oneUI/docs/guides/upstream-sync-reference.zh-CN.md`](../1oneUI/docs/guides/upstream-sync-reference.zh-CN.md)
>
> **✅ 2026-08-21（续）最新交接：1oneUI 136 提交上游同步也已完成并推送**：[`../1oneUI/docs/guides/session-2026-08-21b-1oneui-upstream-sync-handoff.zh-CN.md`](../1oneUI/docs/guides/session-2026-08-21b-1oneui-upstream-sync-handoff.zh-CN.md)——`1oneUI` 的 `one-main` 已推进到 `e613573e1`，两仓同步至此全部完成。下方 2026-08-21 那条「下一步是 1oneUI 135 提交同步，完全未开始」的结论已过时。仍遗留：1oneCore 后端 `cargo test --workspace --no-fail-fast` 完整跑一遍确认到底（上次停在 34 个二进制零失败）。
>
> **🚧 2026-08-21 最初交接（背景）：**[`../1oneUI/docs/guides/session-2026-08-21-1onecore-upstream-sync-handoff.zh-CN.md`](../1oneUI/docs/guides/session-2026-08-21-1onecore-upstream-sync-handoff.zh-CN.md)——149 提交上游合并已完成并推送到 `one-main`（`8df6c094`），全部已知回归已修复；文档里还列了 `cmd_server.rs` 一处被合并静默丢失的优雅关闭逻辑（已修复）等细节。

- **【2026-08-18】视觉委托绕过企业治理已修（P0，已推 `38cdf307`，**已重编内嵌、已真机验证**；本仓 `dream-core-ai-agent`/`dream-core-conversation`/`dream-core-app`/`dream-core-channel`/`dream-domain-billing` + aionrs `051ff54`）**：`ReadImage` 给纯文本模型读图时会委托**一个单独的视觉模型**——这条新的模型调用路径既不过 allowlist，花费也不进账本。①**allowlist 够不到它**：`resolve_vision_delegate`（现居 `capability/vision_delegate.rs`——本轮落在 `factory/aionrs.rs`，随后被桥接降级那轮抽到 capability 层）只查 `enabled`/`model_enabled`/能力判定，而全仓 allowlist 强制点只有 `check_send`/`check_model` 两处、都在「会话模型」那一侧，管理员踢出清单的模型照旧能被当委托调用。修法走 trait 注入（同层不能直接依赖 dream-domain-billing，照 `IpAllowlistGate`/`SendGate` 既有安排）：新增 `dream_core_ai_agent::ModelAllowlistGate` + `AgentFactoryDeps.model_allowlist` + `dream-core-app` 的 `BillingModelAllowlistGate`。⚠️ **`BillingService` 构造因此从 `routes.rs` 上移到 `AppServices`**——agent 工厂在任何 router 存在之前就装配好了；`routes.rs` 改为复用同一实例。⚠️ **刻意用 allowlist-only（`check_model_allowed`）不用 `check_send_allowed`**：委托在 session 构建时解析一次并缓存整个会话，此刻查预算会把「那一瞬间预算耗尽」永久烙进这个会话。②**用量被丢弃**：aionrs 的 `read_image.rs` 写的是 `LlmEvent::Done { .. } => break`。修法跨三仓穿回来——aionrs 侧 `aion-types::DelegateUsageSink`（放 aion-types 是必须的，aion-tools 不能依赖 aion-agent）+ `OutputSink::emit_delegate_usage`（**默认 no-op**，三个既有 sink 零改动）；本仓侧新增内部事件 `AgentStreamEvent::DelegateUsage`（契约同 `SegmentBreak`：relay 消费、永不转发 WS）→ `RelayOutcome.delegate_usage` → `turn_orchestrator::meter_attempt()`。⚠️ **账本记委托模型名不记主模型**（费率表按族名 substring 匹配，记主模型是 misattribution）；**匹配不上只打 warn 绝不发明兜底费率**，`record_turn` 补了与 `record_media_usage` 同款的 zero-cost warn。③顺带修「报错说假话」：被 allowlist 拦掉时原文案是「去设置里加视觉模型」，对被封禁成员既错又无法执行，现经 `vision_unavailable_reason` 换成点名被拦模型 + 找管理员，**但原文那句「不许猜、不许编」在所有路径保留**（有测试钉死）。⛔ **没有放宽 `capability/image_input.rs`**——它锁死纯文本 lookalike 不许误放行，`7cf40b96` 改过被 revert（`a1caef8e`）；本次只在外面再加一道闸。三条负向验证均做过（拆掉闸门 4 红、拆掉用量上报红、拆掉 meter 循环 4 红，各自恢复复绿）。详见：
⚠️ **收尾时踩到一个值得记住的坑**：本轮跨仓开发期间 `Cargo.toml` 末尾挂着临时 `[patch]` 指向 aionrs 工作副本，导致"本机编译通过"完全失去意义——合并推送的那个提交实现了 `OutputSink::emit_delegate_usage`，而 Cargo.lock 仍指向 aionrs `1d485e5`（该方法尚不存在），**干净 checkout 直接编不过**，patch 把它整个藏住了。已由 `38cdf307` 修复。**规矩：工作区里只要有 `[patch]`，"编过"就不是可推送的判据**，必须先删块 + `cargo update` + 重编一次。真机验证记录（allowlist 只留主模型 → 4 个视觉模型逐个被拦、报错说策略原因且保留反编造指令、账本无幽灵行；allowlist 放开 → 委托 `kimi-k2-6` 被选中并真读出图中文字、账本两行且委托行记委托模型名）见交接文档 §0.5：
  [`../1oneUI/docs/guides/handoff-2026-08-18b-vision-delegate-wip.zh-CN.md`](../1oneUI/docs/guides/handoff-2026-08-18b-vision-delegate-wip.zh-CN.md)
  [`../1oneUI/docs/guides/session-2026-08-18-vision-delegate-governance.zh-CN.md`](../1oneUI/docs/guides/session-2026-08-18-vision-delegate-governance.zh-CN.md)
- **【2026-08-08·收尾】allowlist 误伤媒体改为可操作 + 未知模型 0 计价可诊断（本仓 `89029f66`+`af3f7a36`，已推，**已重编内嵌**）**：①allowlist 是聊天与媒体共用一份清单（`check_media_allowed` 刻意如此），代价是管理员填完聊天模型就把全公司图片/视频生成一起关掉，而用户只看到"blocked by company policy"。媒体 precheck 被 `ModelNotAllowed` 拒绝时改为点名杠杆（哪个模型/去哪加/这份清单同时管媒体），其余拒绝原因文案不变。**真机复核**：设只含聊天模型的 allowlist → 新文案出现、清单内模型仍放行 → 已还原。②未知模型计价为 0 时打 warn——0 成本不消耗成本上限，上限对该模型悄悄失效，而费率表按模型名匹配、自带命名的网关一条都匹配不上。⚠️ **刻意不发明兜底费率**（改变管理员上限实际拦什么 = 定价决策），只让它可诊断；新增测试把"未知模型仍为 0"钉死，逼将来改动者明知在做定价决策。③`record_media_usage` 收参为 `MediaUsage` 结构体——上一轮我加到 8 个参数顶破 `too_many_arguments`，按 ratchet 修回；且七个大多原语的值里好几个 `i64`，位置写错能编译却会静静计错账。dream-domain-billing 20 / dream-domain-devops 60 / dream-core-system 172 全绿。
- **【2026-08-08·真机】重编内嵌后真机验证抓出两个缺陷（本仓 `ba545d5f`+`b03d9f82`，已推，**已重编内嵌**）**：①**agent 发起的生成在账本里 `conv=None`**——媒体 MCP 是 stdio 子进程，除 env 无从得知服务哪个会话（`startMediaJob` 注释早写明"渲染层知道，MCP 调用方不知道"），于是最常见那条路恰好留不下踪迹。新增 `AIONUI_MEDIA_CONVERSATION_ID`（照 `AIONUI_MEDIA_WORKSPACE_DIR` 的做法，同样只给内置媒体服务器）。②**走 repo 行进会话时媒体 env 一个都没注入**——注入原本只在**快照**路径，而 `session_mcp` 修复让媒体 MCP 以 **repo 行**身份进会话，`row_to_mcp_server_config` / `row_to_sdk_mcp_server` 都不注入；产物落兜底目录、归因为空。两个工厂的 repo 行路径各补一次，`workspace` 从调用点 `ctx.workspace` 穿进去。⚠️ 我一开始把"图片落错目录"归因于"agent 没传 workspace_dir"，**同一根因、第一次判断错了**。**教训：给既有能力开一条新到达路径时，沿途所有 per-conversation 上下文都要重新数一遍——旧路径上顺手就有的东西，新路径上一件都不会自动跟过来，且缺了不报错。** 真机前后对照：修复前两次 `conv=None`、图落 `%APPDATA%`；修复后 `conv=2693bfb1`、图落会话工作目录。另实证注入结果为 `["aionui-image-generation","ftshare","stock-sdk","team-search"]`——媒体在、同为 builtin+enabled 的 export-pdf / team-knowledge 不在。dream-core-ai-agent 793 绿。
- **【2026-08-08·续】企业模型渠道 Stage B（本仓 `598dd32a`，已推 `origin/one-main`，**改完必须 `backend-rebuild.ps1` 重编内嵌**）**：成员端物化。每个可见渠道落成一条**普通 `providers` 行**，聊天/ACP 桥接/三种媒体适配器形态零改动认得它；只差 `base_url` 指向公司模型代理、`api_key` 是可撤销渠道令牌而非公司凭据。新增迁移 `041_provider_managed_by`（`providers.managed_by`：NULL=自配、`'enterprise'`=下发）、`dream-core-system/src/managed_provider.rs`（provider id 由渠道 id 派生所以是 upsert；authoritative 才对账删除；**同 id 的个人 provider 只记 conflict 跳过，绝不覆盖**）、`POST /api/providers/sync-model-channels`。`ProviderService::update/delete` 对托管行直接拒绝（同步下轮会还原，放行只会产生自己回滚的改动）。⚠️ **顺带修掉一个自己写出的、只在运行时炸的缺陷**：给 provider 的 INSERT 加了第 18 个 `.bind()` 却漏改 SQL 列清单与占位符（仍 17 个）；`sqlx::query` 不做编译期校验所以 `cargo build` 一路绿，是新加的同步测试真的走了 `create` 才撞出来——**给 `sqlx::query` 加 bind 时列清单/占位符/bind 三处必须一起数**。9 条新测试；dream-core-system 172 / dream-core-db / dream-domain-devops 全绿。产品侧进度见 1oneUI 活文档：
  [`../1oneUI/docs/guides/enterprise-china-market-gtm.zh-CN.md`](../1oneUI/docs/guides/enterprise-china-market-gtm.zh-CN.md)
- **【2026-08-08】媒体企业管控修复 + 企业统一下发模型渠道 Stage A（本仓 `dream-domain-devops` / `dream-domain-org` / `dream-domain-billing` / `dream-core-ai-agent` / `dream-core-app` 均有改动，**改完必须 `backend-rebuild.ps1` 重编内嵌**）**：①**内置 MCP 结构上进不了任何会话**——`factory/acp.rs` 与 `factory/aionrs.rs` 都有 `if !selected || row.builtin { continue; }`，唯一通道是前端写会话快照，而前端只在新建会话页写，于是数字员工/定时任务/团队/存量会话**全都不会生图生视频**。新建 `factory/session_mcp.rs` 把两个工厂的选择规则收口一处，媒体这一个内置 MCP 跟着自己的 `enabled` 走，**刻意只放这一个**（否则每个 agent 都拿到 PDF 导出与团队知识库）。⚠️ 顺带发现 **`dream-core-ai-agent` 的 lib 测试目标在主干上编译不过**（`merge_session_snapshot_mcp_servers` 加了 `workspace` 参数没改调用点），那 793 条测试一直没跑过，已修。②`dream-domain-billing::record_media_usage` 补 `conversation_id`（此前对媒体恒写 NULL，账本里的每笔媒体花费都指向不了任何地方）；`dream-domain-org::list_agent_audit` 的 detail 补 `$.args.prompt`/`$.input.prompt`（媒体调用此前只显示工具名与空白）。③**企业统一下发模型渠道 Stage A**：新增迁移 `dream-domain-devops/010_provider_registry`（`one_provider_registry` + `one_provider_channel_tokens`）、`provider_channel.rs`（registry CRUD 复用既有 `member_visibility_where()`/`validate_resource_scope()`，凭据用 `dream_core_common::encrypt_string` 加密、`DevopsService::with_encryption_key` 注入）、`model_proxy.rs`（**路径透传**反向代理 `ANY /api/one/model-proxy/{cid}/{*path}`，双向流式，挂在 CSRF 层之外——调用方是 agent 进程不是浏览器，同 Codex bridge 的既有安排）。⚠️ **刻意不照抄 codex-bridge 的协议感知代理**：媒体一家说三种互不兼容的形状，协议感知要写三遍；路径透传让下游零改动。⚠️ 成员端凭据是**专用可撤销 channel token 不是会话 JWT**（JWT 因成员变动轮换，用它每次人员变动都会打断所有已配渠道），**只存哈希**。⚠️ **移除成员连带撤销 token** 走新 trait `dream_domain_org::CredentialRevoker`（同层不能直接依赖，照 `EnterpriseSync` 既有安排），接在 `invalidate_user_tokens` 里所以每条杀会话的路径都会杀 token。⚠️ `one_devops_service` 因此**必须在 `one_org_service` 之前构造**（routes.rs 里已移位并留注释）。dream-domain-devops **64 测试全绿**（60 单测 + 4 条打真实上游服务器的端到端），含"把渠道列表序列化后逐字节搜真实 key"那条；端到端验的是设计赌注本身——三种协议形状路径原样抵达、成员 token 被换成公司凭据且自己不外泄、撤销后立即在代理层被拦（凭据替换那条做过负向验证）。**Stage B（客户端）未做。** 产品侧完整盘点见 1oneUI 那份活文档：
  [`../1oneUI/docs/guides/enterprise-china-market-gtm.zh-CN.md`](../1oneUI/docs/guides/enterprise-china-market-gtm.zh-CN.md)
- **【2026-07-31 最新·四会话统一收尾】5 处修复 + 三仓对齐（本仓 `ff28457e` / `569def58` / `44caf9f9` / `7644bcfc` / `82e268f5`，均已推 `origin/one-main`）**：07-30~31 四条并行会话结束后由一条会话统一收尾，**下面每条都已完成，别再重做**。①**`dream-domain-billing` 越权此前只推了一半**——07-30 的文档写着"两处越权都已收紧"，但 `985f86a9` 实际只含 dream-domain-org 备份端点那半边，`is_billing_admin` 主干上仍是 `system_admin | org_admin`，任何项目组管理员都能改整个企业的订阅档位/席位/成本上限。已收紧为企业作用域并补测试（`ff28457e`）。**教训：会话文档写"已修"≠已推，收尾时必须拿主干代码逐条验。** ②**`dream-core-runtime` 在 Windows 上首次全绿**（85 passed/8 failed → **93/0**，`569def58`）：8 条全是测试写死 Unix 假设（夹具用 `bin/node` 而 Windows 归档是根目录 `node.exe`、路径过滤硬编码正斜杠、断言 std `Command` 的 Unix 专有 Debug 渲染），产品代码无缺陷。**统计口径：报既有失败数必须跑完整 crate，带模块过滤会得到偏小的数字。** ③**从界面删除的 MCP 不再被会话重新注入**（`44caf9f9`）：`list_by_ids_any` 不过滤 `deleted_at`，会话只要在 `mcp_server_ids` 里还记着 id 就继续加载它，用户视角是"删了还在拖慢会话"；`any` 指的是忽略 `enabled`（会话选中的 server 在全局禁用后仍归它用），删除是终态，两者不该共用豁免。四个生产调用方全是"注入/展示会话选中的 server"，无一需要已删除行。④**ACP 会话 id 推迟到首轮对话之后再持久化**（`7644bcfc`，修正 `2fa3d229`）：`open_session_new` 在 `session/new` 返回瞬间就落库 id，而未被 prompt 过的会话在 CLI 侧无痕迹、resume 必失败，**且失败前 CLI 会把整个会话连同 MCP 栈完整建一遍**——实测一次 ACP 团队冷启动 44.8 秒里 26.6 秒（59.5%）花在这次注定失败的尝试上，救援重建的新 id 又继承同样状态，全月 12 次 `stale session id rejected` 由此而来。已拆出 `AcpSessionEvent::SessionIdDurable` 承载"值得持久化"，与"id 已生效"分开。**⚠️ 第一版 `2fa3d229` 改错了地方**（改的是给前端事件流的 `AgentStreamEvent::SessionAssigned`，而落库走聚合事件 `AcpSessionEvent::SessionAssigned`），单测全绿却毫无作用，**是真机验证抓出来的**——记在这里当反面样本。**真机 CDP 实测**：ACP 团队冷启动 **45.4s → 5.8s**、stale 拒绝 0 次、新建团队后未发消息则 `acp_session` 连行都不写。⑤**三仓与两个主检出全部对齐**：1oneUI 主检出由"落后 73+ 提交/60 个未提交"→ 零差异（丢弃的那批经抽查是 `bun run format` 残留，其中一处正在**回退 `237495dd`**，即另一会话警告过的暂存区陷阱，已实锤）；1oneCore 由 `sync-v0153` 切回 `one-main` 且零未提交；`1oneUI/.sync-worktree` 已彻底删除（删前确认：是 worktree 非业务目录、`sync-v2142` 相对主干零独有提交、构建脚本无引用、备份齐全）。⑥**provider 失效时不再把内部错误串裸露给用户**（`82e268f5`）：队友的 provider 被删或改配置后每次 warmup 都失败，而这条路径报的是**类型化**的 `ConversationError::ProviderNotFound`，`map_conversation_update_error` 却只给 `BadRequest` 那条臂做了友好化，类型化变体直接落进 `other => InvalidRequest(other.to_string())`，于是它的 Display「`Provider '<id>' not found`」被原样交给用户——一个看不懂也无从下手的内部 id。已补上类型化分支并与 `BadRequest` 共用同一句提示。**⚠️ 这个盲区之所以长期没被发现，是因为原有两个单测都手搓 `BadRequest`，而真实 warmup 路径走类型化变体**，分支缺失时没有任何测试会红；新增的用例已反向验证（移除修复后确实报 `Provider 'ff9e8905' not found`）。与 1oneUI `c6b1284d7` 是同一症状的不同环节：那条修「专家带失效模型导致建团队被拒」的前置成因，本条修「provider 事后失效时报错怎么展示」。**📋 07-30~31 全部未决项的唯一归一清单在 `D:\aionui-m0\HANDOFF-2026-07-30-todos.md`**（工作区根，非仓库），现仅剩两项：给托管 node 目录加杀软排除、License 公私钥离线轮换，均属人工执行。详见 1oneUI 那份文档：
  [`../1oneUI/docs/guides/session-2026-07-30-team-warmup-cold-start.zh-CN.md`](../1oneUI/docs/guides/session-2026-07-30-team-warmup-cold-start.zh-CN.md)
- **【2026-07-31】数字员工接上专家库与模型（本仓 `crates/dream-domain-employee` + `dream-core-app/router/routes.rs` 一行接线）**：修 aionrs 数字员工 100% 必挂的 `Provider '' not found`。根因=`dream-domain-employee/service.rs` 的 `provision_run` 硬写 `model: None` + `assistant: None`，会话 `model` 列 NULL → `dream-core-conversation/task_options.rs:42` 返回哨兵 `provider_id: ""` → `factory/aionrs.rs:81-87` 空串查 provider 失败（ACP 不受影响：`factory/context.rs:8-13` 的 `FactoryContext` 结构上没有 model 字段）。**修法逐条照抄 `dream-core-cron/executor.rs`**：`resolve_model()`（只有 aionrs 返回顶层 model，965-970 行）、`build_assistant_request()`（`assistant_id` 优先、legacy `custom_agent_id` 兜底，1070-1090 行）、`r#type = if assistant.is_some() { None } else { Some(agent_type) }`（553 行）。**两条必须记住的约束**：①顶层 `model` 只允许 aionrs，其他类型传了 `ConversationService::create` 直接 400（`dream-core-conversation/service.rs:795-802`）；②有 `assistant` 时 `r#type` 被忽略、effective type 由人设快照推导，**手动改后端唯一生效通道是 `AssistantConversationOverridesRequest.agent_id`**（优先级见 `dream-core-conversation/service.rs:1473-1477`）。新增迁移 `employee_004_persona_model`（`assistant_id`/`agent_id_override`/`model_id`/`model` 四列，`agent_type` 保留继续做 model 门控）+ `validate_model_binding()` 保存期三规则（aionrs 必须有模型、非 aionrs 不许带模型、模型须来自已启用 provider；第一条只在请求真的动绑定时强制，故存量 backend-only 员工改名不被拦）+ `with_provider_repo()` 可选 builder。**一处 `provision_run` 修复覆盖全部 4 个建会话入口**（手动/devops 派发/devops 拆解/cron 扫描全走同一函数，employee 的定时路径没有第二套模型解析）。`cargo test -p dream-domain-employee` 17 绿；真机 29 条断言全绿（核心判据：报错从 `Provider '' not found` 变成真实网络调用失败）。改完 Rust 必须重编 aioncore。详见 1oneUI 那份文档：
  [`../1oneUI/docs/guides/session-2026-07-31-digital-employee-expert-model-binding.zh-CN.md`](../1oneUI/docs/guides/session-2026-07-31-digital-employee-expert-model-binding.zh-CN.md)
- **【2026-07-30】团队唤醒 38 秒冷启动定位与修复（本仓 `dream-core-runtime` 提交 `ad407cdb` + aionrs `02f3c50`，均已推）**：用户反馈"拉起团队要花很长时间"。**慢的不是团队功能**——`dream-core-team` 的会话骨架 2 毫秒就起好（MCP server + leader attach 开始），38.3 秒全在 leader 成员的 agent 运行时上，且队友是懒唤醒的（冷启动只 attach lead，其余广播 `Dormant`）。**①`node_runtime/mod.rs` 的 `cached_managed_runtime()` 缓存了 `ResolvedNodeRuntime` 却没缓存"它已被验证过"**，每次命中仍重跑 `validate_managed_runtime()` → spawn `node`/`npm`/`npx` 三个 `--version`（实测 6.6s，**每个 agent 冷启动都付一次**）。已改为带 `validated_at` 的 `CachedManagedRuntime`，TTL 5 分钟内只走新增的 `revalidate_managed_runtime_files()`（复用生产的 `runtime_from_root()` 做文件系统校验，version 沿用上次完整验证的值），实测 **6.6s → 7ms**；轻量路径刻意**不刷新 `validated_at`**（否则完整探测永不再跑，有断言锁死）、也不发 `Validating` 进度事件（核实过该 phase 只映射 UI 展示无逻辑依赖）。**②aionrs `aion-mcp::connect_all` 要把所有连接排干才返回**（结果直接喂 `register_mcp_tools()` + `load_skills()`），`DEFAULT_STARTUP_TIMEOUT_MS` 就是"用户等多久会话才可用"的上界，30s 被一个起不来的 `stock-sdk` 每次吃满，已降到 **15s**。真机 CDP 实测 **38.3s → 15.5s**，热路径 1.1s，ACP 团队路径无回归。**⚠️ 真正的底层根因在代码之外**：这台机器上启动一个 node 进程要 **1~1.7 秒**（正常几十毫秒，Defender 实时防护逐进程扫 89.8MB 的 `node.exe`），`npx -y <pkg>` 是 npx-cli→npm 解析→联网 revalidate→再 spawn 的多层 node 叠加，所以慢且不稳——**治本是给托管 node 目录加杀软排除，属安全设置需人工执行**。**⚠️ 引入一处功能性回退**：`stock-sdk` 慢时会被 15s 砍掉，69 个股票工具不可用。**发现的既有缺陷（未修）**：`list_by_ids_any` 不过滤 `deleted_at`（有测试 `list_by_ids_any_includes_soft_deleted_rows` 锁死该语义），导致**从 UI 删掉的 MCP 只要会话 `mcp_server_ids` 还记着 id 就照旧注入**（清脏数据必须硬删）。**统计口径提醒**：`cargo test -p dream-core-runtime` 在 Windows 上本轮由 **85 passed / 8 failed → 87 passed / 6 failed**（顺带修好 2 个夹具按 Unix 布局写的既有红）；只跑 `node_runtime` 过滤会得到 3 红的不同数字，**报既有失败数必须跑完整 crate**。详见 1oneUI 那份文档：
  [`../1oneUI/docs/guides/session-2026-07-30-team-warmup-cold-start.zh-CN.md`](../1oneUI/docs/guides/session-2026-07-30-team-warmup-cold-start.zh-CN.md)
- **【2026-07-30】企业版商业化 5 项待办后端落地（本仓 dream-domain-org/dream-domain-devops/dream-domain-enterprise/dream-core-app 改动，提交 `092e9071` + `985f86a9`，✅ 均已在 `origin/one-main`）**：P0-2 移除成员（`dream-domain-org::remove_member` 以 `leave()` 为模板——删成员行 + 重选激活组 + **轮换 JWT 让离职者会话立即失效** + 审计**归因于操作者**；三道守卫：不能移除自己/复用 `ensure_not_last_admin`/非 system_admin 不可移除 system_admin；`dream-domain-enterprise::remove_member` 删行即释放席位）、P1-2 资源接管（**两套所有权字段**：`created_by` 三个 registry + `creator_id`&冗余 `creator_name` 五张看板表必须同改；只有 registry 有租户列，五张看板表是部署级全局的，"跨组不能转"实质只在 registry 成立）、P1-1 备份恢复（新模块 `dream-domain-org/src/backup.rs`；列名走 `PRAGMA table_info` 运行时读取以免迁移增删列后悄悄漏字段；**凭据一律脱敏**，dream-domain-org 同层不能依赖 dream-domain-sso 的私有 `secret_keys()`，故用超集清单+模式匹配双保险）、**P0-3 知识库改混合检索**（新模块 `dream-domain-devops/src/retrieval.rs`：SQLite 内置 FTS5 出 BM25，与向量 cosine 用 RRF 融合；**FTS 表运行时创建并逐级降级**——trigram 优先以支持中文分词，放进迁移会让缺 FTS5 的构建启动即崩；MATCH 表达式对用户输入做短语引用转义防算子注入；删文档时**先**清词法索引，否则正文永久留盘）。**⚠️ LanceDB 方案已被推翻**（实测 aioncore.exe 94.3MB→299.3MB，且 `lance-encoding` build script 硬依赖 `protoc`）。**⚠️ `RagSearchHit.score` 刻意保持为 cosine**：`dream-domain-devops/routes.rs:302` 派发任务注入知识库时按 `>= 0.35` 筛选，改成 RRF 分数（量级 0.03）会让该链路**静默失效**。测试 dream-domain-devops 46 / dream-domain-org 41 / dream-domain-enterprise 13 全绿。**⚠️ 同日后半段修掉两个越权（`985f86a9`）**：判据统一为「守卫必须匹配它触碰的数据的作用域」——①备份端点是本轮自己写出来的越权（挂 `RequireOrgAdmin`，但 `backup.rs` 导出的是**整个部署所有租户**，有测试 `bundle_spans_every_tenant_not_just_the_callers` 明确锁死该语义，等于任何一个项目组管理员都能导出并覆盖导入别的组的全部数据），已收紧 `RequireSystemAdmin`；②`dream-domain-billing` 的管理员判定原为 `system_admin | org_admin` 是**既有**越权（订阅档位/席位上限/成本上限全是企业级数据，组管理员能改整个公司的），已收紧为仅 `system_admin` 并补测试 `billing_admin_is_enterprise_scoped_not_project_group_scoped`。另：原文档 §7.1「未合主干」已解除，另一会话收尾上游同步时合入，端点在主干可查。详见 1oneUI 那份文档：
  [`../1oneUI/docs/guides/session-2026-07-30-enterprise-backlog-and-hybrid-rag.zh-CN.md`](../1oneUI/docs/guides/session-2026-07-30-enterprise-backlog-and-hybrid-rag.zh-CN.md)
- **【2026-07-30】`dream-core-extension` 的 Windows 欠账 12 条全修（本仓 `lifecycle.rs`/`skill_service.rs` + 两处测试改动，提交 `4821b65d`）**：承接 07-29 同步的遗留。**先纠正数字**：此前报「5 条」是因为 `cargo test` 默认 fail-fast，lib 目标一挂就不跑后面的集成测试二进制；加 `--no-fail-fast` 真实是 **12 条**（lib 5 + `tests/lifecycle_hooks_test.rs` 7）。**统计失败数必须带 `--no-fail-fast`。** ①**产品缺陷：扩展生命周期钩子在 Windows 上从来没能运行**（11 条同源）——`execute_hook` 直接 spawn 脚本靠 shebang 找解释器，实测 `CreateProcess` 拒绝 `.sh`（`EFTYPE`/errno `-4028` = `ERROR_BAD_EXE_FORMAT`）；新增 `build_hook_command()` 按扩展名分发（`.sh`/`.bash`→POSIX shell，`.ps1`→`powershell -File`，其余照旧直接 spawn），**顺带解掉 Unix 上的隐患**（扩展以压缩包分发常丢可执行位），找不到解释器时给明确原因而非 OS 错误码。②**悬空链接导入：`replace_existing_path` 里那段 Windows 分支是死代码**——它写 `if metadata.is_dir()`，但实测悬空 junction 的 `symlink_metadata` 是 `is_symlink=true / is_dir=false / file_type().is_dir()=false`（**Rust 在 Windows 上对任何链接 `is_dir()` 都返回 false**），分支从不触发，照旧落到 `remove_file` 报 os error 5。改为不依赖 `is_dir()`：先试 `remove_file`，失败再 `remove_dir`（目录型 reparse point 只有后者能解，且不跟随链接故目标已删也能删）。**⚠️ 教训：平台特有分支的判断条件本身也必须实测——它可以完全合理却永远为假，而测试只会说「还是坏的」，不会说「你的修复根本没跑」。** ③**两个 timeout 测试原本压根没测产品代码**（绕开 `execute_hook` 自己 timeout 一个裸 `Command`，既没断言本 crate 行为也跳过解释器分发）——新增 `execute_hook_with_timeout()`（内置最短超时 30s 测试无法触发）改为驱动真实路径并断言 `HookTimeout` 字段。④顺带：`092e9071` 给 `dream-domain-devops` 加了 `futures-util`/`tempfile` 却没提交 `Cargo.lock`（每次构建弄脏工作区，`--locked` 会失败），已补。`cargo test -p dream-core-extension --no-fail-fast` → 18 个测试二进制全绿。详见 1oneUI 那份文档 §6：
  [`../1oneUI/docs/guides/session-2026-07-29-upstream-sync-v028-v0153-v2143.zh-CN.md`](../1oneUI/docs/guides/session-2026-07-29-upstream-sync-v028-v0153-v2143.zh-CN.md)
- **【2026-07-29】三仓上游全量同步（本仓 v0.1.48→v0.1.53，60 提交，23 个冲突文件）**：**①不采纳 session-port 迁移（#609）**——上游把 Claude/Codex 迁到 direct-CLI `SessionAgentTask`，但那条路径在 `factory/acp.rs` early-return，**绕过了 fork 的 Codex/Claude 桥接注入**（`codex_bridge_config_repo`/`claude_bridge_config_repo`），上游只接了第三方 cc-switch 兜底，照搬会让桥接静默退回 cc-switch-only。用户拍板不迁移：`factory/acp.rs` 删掉分发块、`AgentFactoryDeps` **不加** `session_spawner`、`services.rs`/`factory_provider_integration.rs` 移除 spawner 装配（三处均留交叉注释）；`session_agent.rs`/`dream-core-session`/`dream-core-process` 照常合入但**未接线**。**②`acp_tool_runtime` 被 git 静默删除（零冲突标记）**——自动合并把 fork 独占的 claude-agent-acp/codex-acp **按需 npm 下载**逻辑整个换成上游的 `managed_cli`（原生二进制、打包期预备），而 `factory/acp.rs` 仍在调它，靠**编译报错**才暴露；已恢复该模块 + `cache.rs::managed_acp_tool_root()` + `managed_resources.rs::acp_tool_sources()` + `runtime_status.rs` 四个 reporter/mapper + `RuntimeResourceKind::AcpTool`；只有 `managed_acp_tool_contract_for_export` 未恢复（上游 `cmd_prepare_managed_resources.rs` 已只产出 schema-v2 契约，旧函数无调用方也无对应 schema），`managed_cli` 保留未接线。**⚠️ 教训：3-way merge 能在零冲突标记的情况下删掉 fork 独占模块——"冲突解完"≠安全，必须全量编译+跑测试。** **③migration 撞号**：上游 026/027/028/029 与 fork 已占编号冲突，重排为 **037/038/039/040**；另有一个与 fork `030_*` 逐字节相同的 `025_sync_and_add_acp_registry_agents.sql` 直接删除；`provider_model_settings_migration.rs` 里按编号回放的 25/27 同步改 37/38。**④其他裁决**：per-model 能力配置（`ModelCompatOverrides`）经 `git log --grep` 核实是上游真新功能非 fork 重复，正常吃并把 fork 独有的 `resolve_provider_config_for_bridge` 接上新解析链；`cli_probe.rs` 取上游 `validate_with_budget`；`agent_test.rs` 保 fork 的 `Some(32_000)` 断言（锁 `33c2bd2`）；`Cargo.lock` 34 个冲突块直接重新 `generate-lockfile`。**验证**：31 crate / 178 测试二进制 / **5788 用例通过**，clippy(碰过的 16 crate) + fmt 绿；失败项全部定性（本轮引入的 3 条已修，其余为 Windows 既有/上游 macOS 硬编码路径）。**⚠️ 推送前必做**：`Cargo.toml` 末尾有临时 `[patch]` 指向本地 `../aionrs-local`，须**先推 aionrs → 删 patch → `cargo update -p aion-mcp` → 再推本仓**。详见 1oneUI 那份文档：
  [`../1oneUI/docs/guides/session-2026-07-29-upstream-sync-v028-v0153-v2143.zh-CN.md`](../1oneUI/docs/guides/session-2026-07-29-upstream-sync-v028-v0153-v2143.zh-CN.md)
- **【2026-07-29】ftshare/stock-sdk 加入内置默认 MCP 列表（前端在 1oneUI）+ aionrs 单工具级 schema 过滤修复（本仓 `Cargo.lock` 对齐 aionrs 新 commit，无本仓源码改动）**：用户确认股票专家用的 ftshare/stock-sdk 两个 MCP 没存密钥，真机 CDP 核实属实（`stock-sdk` 是 `npx -y stock-sdk-mcp` 公开包、`ftshare` 是纯网关 URL 无 headers），前端已加进默认种子列表（同 `chrome-devtools` 写法）。**用户追问已知的 07-27 ftshare 工具名不合法 bug 能不能修**——真机核实上游 `market.ft.tech` 的 schema bug 本身没修，只是 07-27 那道防线（server 级排除）挡住了崩溃感知；架构上核实 **ACP 路径（Claude Code/Codex CLI）做不到单工具过滤**（工具发现在 CLI 子进程内部,本仓管不到,见 `factory/acp.rs:500-520` 注释），但 **aionrs 路径不同——aionrs 自己拥有 MCP 工具发现逻辑（`aion-mcp` crate），是 fork 自己的代码，改了安全**。已在 aionrs 仓库新增单工具级 schema 过滤（172 个工具只丢 `ft_goodwill_market_overview` 这一个坏的，其余 171 个照常注册），推送 `gaogg521/aionrs` master `347348f`；本仓 `Cargo.lock` 已 `cargo update -p aion-mcp` 对齐 + 重编 `aioncore.exe` 换入本机开发环境（换入时开发环境本就已停跑，未做真机复测，留给用户下次启动自测）。详见 1oneUI 那份文档：
  [`../1oneUI/docs/guides/session-2026-07-29-marketplace-audit-and-mcp-tool-filter.zh-CN.md`](../1oneUI/docs/guides/session-2026-07-29-marketplace-audit-and-mcp-tool-filter.zh-CN.md)
- **【2026-07-28】专家市场（Expert Marketplace，架构推翻重做过一次）+ 会话级 agent_id 覆盖（本仓 `dream-core-assistant`/`dream-core-db`/`dream-core-conversation`/`dream-core-api-types` 改动，前端在 1oneUI）**：`AssistantConversationOverridesRequest` 新增 `agent_id`，解耦"选人设"与"选后端"。**第一版**用 `AssistantService::import_personas` 把 281 个 WorkBuddy 人设直接物化进 `assistant_definitions`（`source='imported'`）——真机验证时用户看到"我的助手"从 5 条变成 286 条，当场要求"单独建一个表"。**第二版（当前状态）**：新增完全独立的 `assistant_marketplace_personas` 表（迁移 035）+ `IAssistantMarketplaceRepository`，目录内容随二进制打包（`crates/dream-core-app/assets/marketplace-personas/`，照抄 `builtin.rs` 的 `include_dir!`，新模块 `dream-core-assistant/src/marketplace.rs`，启动时幂等物化——全新装机也能看到完整目录）；新路由 `GET /api/assistants/marketplace` + `POST .../{id}/install`（安装直接复用已有的 `import_personas()`）。刻意不塞进已经 6479 行超标 6.5 倍的 `service.rs`。⚠️ 顺带修的遗留：`AssistantSource` 枚举只有 3 变体，`'imported'` 在四处分散的字符串→枚举映射里被 collapse 成 `"user"`，加 `Imported` 变体后编译器揪出 10 处非穷尽 match 全部修正。真机验证含完整安装链路（UI 点击→后端行→GET 核实→清理）+ 旧的 281 条误导入数据已清理。`ca401c68`→`19461d24`→`52a00adf`。**⚠️ 同日再晚些时候：市场数据源又换了一批（281→252，本仓新增字段）**——`display_name`/`role_name`/`category`/真实头像（迁移 036，`assistant_marketplace_personas` 加列）；`marketplace.rs` 新增 `marketplace_avatar_bytes()` + `materialize_marketplace_personas` 补 `repo.delete_missing()`（换 manifest 不会再留孤儿行，上一批就是没有这一步才留下的教训）；`marketplace_install` 装完后额外用新增的 `AssistantService::set_avatar_from_bytes()` 把内嵌头像写进真实助手记录；新路由 `GET /api/assistants/marketplace/{id}/avatar`。`bbdb0029`。前端配套改动（聊天框"+"菜单专家选择器新功能、IME 闪退修复、专家搜索范围打通）纯 1oneUI 侧，详见文档：
  [`../1oneUI/docs/guides/session-2026-07-28-persona-import-and-backend-decouple.zh-CN.md`](../1oneUI/docs/guides/session-2026-07-28-persona-import-and-backend-decouple.zh-CN.md)
- **【2026-07-27】MCP 管理越界写用户真实 `~/.claude.json` 修复（`dream-core-mcp/adapters/claude.rs` + `dream-core-common/agent_bridge.rs` + `tool_schema.rs`）+ 一键导入解析 BUG 修复（`dream-core-mcp/service.rs`）**：用户自写 MCP（ftshare 含非法参数名占位符）装上后每条消息 400 崩溃。**①配置隔离**：`ClaudeAdapter` 的 CLI 调用没带 Claude 桥接 spawn agent 时用的隔离 `CLAUDE_CONFIG_DIR`，`claude mcp add-json -s user` 越界写了用户自己的真实配置。修复为读写刻意不对称——`detect_existing`（一键导入数据源）仍读真实家目录、`install_server`/`remove_server` 只写隔离目录，隔离路径抽到 `dream-core-common/agent_bridge.rs` 与 agent 工厂共用防漂移；新增 `claude_config_isolation.rs` 直接调真实 `claude` 二进制验证（Claude CLI 是否认 `CLAUDE_CONFIG_DIR` 是它自己的行为，代码上推不出来必须实测）。**②工具 schema 校验闸门**：新增 `dream-core-common/tool_schema.rs` 按 `^[a-zA-Z0-9_.-]{1,64}$` 递归校验，`load_user_mcp_servers` 跳过带毒的整个 server（ACP 不下发单个工具，只能整 server 拦）。**③一键导入两个真 bug**（同天用户顺手点导入又发现）：`service.rs` 的 `shell_split` 把 `\` 当 POSIX 转义符吃掉 Windows 路径分隔符（`cfg!(windows)` 门控修复，同时修好 `sync_team_servers` 复用处）；`SPLITTABLE_STDIO_LAUNCHERS` 白名单太窄导致整条命令行塞进 `command`（`detect_existing` 改为叠加 `~/.claude.json` 结构化字段按名字覆盖猜测结果，匹配不到静默回退不影响连通状态）。真机 CDP 全链路验证：ftshare 172 工具精确标出 1 个、勾两个 MCP 发消息 0 个 400；`codegraph`/`one-image-generation`/`one-web-tools` 三个此前解析坏的 server 重新导入 command/args 结构正确、Windows 反斜杠完整保留；全程 `~/.claude.json` 的 `mcpServers` 字段逐字节未变。前端一键导入弹窗加单独勾选详见 1oneUI 文档。**④ Codex 同款收尾**：`CodexAdapter` 没有 Claude 那种"配置分裂"（agent spawn 本就没隔离 Codex 的 MCP 配置），唯一问题是写操作会碰用户真实 `~/.codex/config.toml`，同病同治;`install_server`/`remove_server` 无生产调用方,隔离不需要碰 agent spawn,不强制用户重新登录 Codex。真机测试当场揪出 codex CLI 不像 claude CLI 会自动建目录,硬失败"CODEX_HOME points to..., but that path does not exist",补了 `ensure_isolated_home()`。新增 `codex_config_isolation.rs` 直接调真实 `codex` 二进制验证。1oneCore `835af6da` + `5a0f8e4c` + 本节其余提交：
  [`../1oneUI/docs/guides/session-2026-07-27-mcp-config-isolation-and-import-fixes.zh-CN.md`](../1oneUI/docs/guides/session-2026-07-27-mcp-config-isolation-and-import-fixes.zh-CN.md)
- **【2026-07-23】Claude 桥接被本机真实 `~/.claude/settings.json` 静默覆盖的严重 bug 已修（本仓 `factory/acp.rs` 改动）**：用户凭真机截图敏锐怀疑"Claude 读的是我本地 settings.json"——**怀疑完全正确**。根因分两层：①`claude-agent-acp` 包装层硬编码 `settingSources:["user","project","local"]`，此前用 `CLAUDE_CONFIG_DIR` 隔离目录本以为已解决；②但**该隔离不完整**——反解包 SDK 自身字符串表确认 `ANTHROPIC_DEFAULT_HAIKU_MODEL`/`_SONNET_MODEL`/`_OPUS_MODEL`/`ANTHROPIC_SMALL_FAST_MODEL` 是独立于 settings.json **文件**之外、直接从进程环境变量读取的模型别名，且这些变量在这台机器的进程树里本来就是"环境态"存在（继承自操作者自己的真实 Claude Code 环境），`dream_core_runtime::agent_process_env()` 从不清理它们，导致会话自己的 `/model haiku` 启动引导会解析出真实 settings.json 里的 `ANTHROPIC_DEFAULT_HAIKU_MODEL`(`ＤｅｅｅｐＳｅｅｋ-v4-pro-claude`)。已在 `resolve_claude_bridge_env()` 里把这 4 个别名环境变量全部钉死为桥接自己配置的模型。**真机双重验证**：修复前诊断会话的真实 transcript 文件里能看到 `/model haiku` 解析出真实设置的别名；修复后重新发一条真实消息，transcript 里既没有该启动引导、也没有真实 settings.json 任何字符串（haiku/两个全角模型名/`litellm-internal`/真实 token 全部 0 匹配），响应用的模型字段正确显示为桥接配置的模型本身。方法论：不要只看 UI 展示的模型名（Guid 页头部模型 pill 走的是独立展示层，与实际生效 provider 不一致是已知纯展示问题，不能作为判据）——唯一可信证据是磁盘上的真实 session transcript：
  [`../1oneUI/docs/guides/session-2026-07-23-codex-bridge-and-injection-fix.zh-CN.md`](../1oneUI/docs/guides/session-2026-07-23-codex-bridge-and-injection-fix.zh-CN.md)
- **【2026-07-23】P0-2 通用 SSO 第一步 OIDC（本仓 dream-domain-sso 新增 `providers/oidc.rs` + models/service/routes 加 Oidc 臂）**：补标准 OIDC provider 一次打通 Okta/Azure AD/Google（都说 OIDC）。套现有 provider 抽象：`discover`(GET `{issuer}/.well-known/openid-configuration`) → `build_authorize_url`(授权码) → `exchange_code`(POST token 端点 form-urlencoded) → `fetch_user_info`(GET userinfo 端点 Bearer) → claims 映射 `ProviderUserInfo`(`company_claim`→org_external_id 同公司自动入伙,对齐飞书 tenant_key)。`SsoProviderKind` 加 `Oidc`；`parse_oidc_config`+`has_minimal_config`/`secret_keys` 加 oidc 臂；authorize/callback 分发加 Oidc 臂。**无迁移**(provider 表 config opaque JSON)。**安全 v1**=身份走 userinfo 端点(access_token 可信)，未做 id_token JWKS 签名校验(列后续硬化)。个人版零影响(provider 全管理员显式配置默认关)。dream-domain-sso **53 单测全绿**(+7 wiremock)。改完 Rust 必须重编 aioncore：
  [`../1oneUI/docs/guides/session-2026-07-23-oidc-sso.zh-CN.md`](../1oneUI/docs/guides/session-2026-07-23-oidc-sso.zh-CN.md)
- **【2026-07-23】多成员模型(方向 B / Phase 2，本仓 dream-domain-org/dream-domain-sso/dream-domain-enterprise/dream-domain-devops 改动)**：`one_user_org` 主键 `user_id` → 复合 `(user_id, tenant_id)`（迁移 007 表重建），新表 `one_active_tenant`（每用户一个激活组，服务端存储）。**关键=active-tenant 服务端按 user_id 存储让 `tenant_of`/`effective_role` 签名全不变**（`OrgActor`/rbac.rs 零改动、`OrgTenantResolver`→team 资源自动跟随激活组）。dream-domain-org 新 `active_tenant_id`/`list_memberships`/`set_active_tenant`、`join_with_invite` 去「已在任一企业则拒绝」总闸改「已在该组则拒绝」+ 设激活、`leave(user_id, tenantId?, code)` scoped 退组 + 退激活重选、`create_tenant_for_enterprise` 删过时 PK 防护、`/api/one/org/{my-tenants,switch}` 新路由。三处跨 crate 直读 `one_user_org.role WHERE user_id`（dream-domain-sso `effective_role`/dream-domain-enterprise `caller_is_system_admin`/dream-domain-devops `user_org_role`）改「激活组优先、否则最近加入」单条 SQL（三处测试 setup 补建 `one_active_tenant`）。**红线**：个人版无成员行→`active_tenant_id` 回落 default、`one_active_tenant` 恒空零写，锁死测试全绿（新增 `active_tenant_defaults_when_no_membership`）。single-membership `create_tenant`+D3 逐字节保留。单测全绿(dream-domain-org 28/dream-domain-sso 46/dream-domain-enterprise 12/dream-domain-devops 23)，**改完 Rust 必须重编 aioncore 才生效**：
  [`../1oneUI/docs/guides/session-2026-07-23-multi-membership-phase2.zh-CN.md`](../1oneUI/docs/guides/session-2026-07-23-multi-membership-phase2.zh-CN.md)
- **【2026-07-23】Codex 桥接注入机制根治 + Claude 桥接新落地(本仓新增 `dream-core-claude-bridge` crate + `factory/acp_launch_policy.rs`/`acp.rs` 改动,详情在 1oneUI)**：①内置 Codex CLI 实际 spawn 的是托管的 `@agentclientprotocol/codex-acp` 包装层，完全独立于系统 `codex`/`~/.codex/config.toml`；该包装层自己的 argv 解析只认 `login`/`cli`/`--version`，此前用 `-c key=value` 注入桥接 provider 配置**全部被无声丢弃**，真正读取方式是 `CODEX_CONFIG`(JSON)+`MODEL_PROVIDER` 两个环境变量——已重写注入机制，顺带修复同一套失效机制下的既有沙箱权限模式注入。②新增 `dream-core-claude-bridge` crate 给 Claude Code 做等价能力——比 Codex 简单（Claude 原生说 Anthropic 协议，不需要本地转发，直接解密保存的 provider 注入 `ANTHROPIC_BASE_URL`/`ANTHROPIC_AUTH_TOKEN`/`ANTHROPIC_MODEL`），顺带发现 Claude 的自定义模型此前完全依赖外部第三方工具「cc-switch」（本仓零写入路径），现有第一方配置优先、外部工具降级为 fallback。真机 CDP + 诊断日志双重验证两条桥接均生效（不再是 OpenAI/Anthropic 官方账号鉴权错误）：
  [`../1oneUI/docs/guides/session-2026-07-23-codex-bridge-and-injection-fix.zh-CN.md`](../1oneUI/docs/guides/session-2026-07-23-codex-bridge-and-injection-fix.zh-CN.md)
- **【2026-07-21】macOS 老系统托管 Node 运行时兼容修复(本仓 dream-core-runtime 改动) + Mac 打包签名公证/发布链路排障(大部分在 1oneUI)**：真机（M1 2020 / macOS Big Sur 11.6）装包后报"托管 Node 运行环境无法启动"，追到 [`node_runtime/mod.rs:228`](crates/dream-core-runtime/src/node_runtime/mod.rs) 的 `validate_runtime()` 真的在 spawn `node --version`；推断内置 Node 24 用较新 Xcode 工具链的 chained-fixups 格式老系统 dyld 加载不了。已给 macOS 单独把托管 Node 降到 22.11.0（Windows/Linux 不变，commit `1797fcf7`；`PlatformSpec` 新增 `node_version` 字段替代全平台共享常量）。**✅ 已在真机确认修复生效**（报错消失）。顺带切了正式 Release `v0.1.49-one.1`（6 平台资产齐全）。Apple 签名/公证配置、bundle ID 改名、发布流水线排障（`Build and Release` 打 tag 自动构建从未真正触发过等）主要发生在 1oneUI，详见：
  [`../1oneUI/docs/guides/session-2026-07-21-mac-signing-and-node-compat.zh-CN.md`](../1oneUI/docs/guides/session-2026-07-21-mac-signing-and-node-compat.zh-CN.md)
- **【2026-07-21】aionrs 侧修复长 Write 工具调用截断后静默丢失(本仓仅 `Cargo.lock` 对齐新 commit `33c2bd22`,无源码改动)**：用户抓包实测定位；`openai_defaults()` 补 `default_max_tokens` + 新增 `LlmEvent::ToolCallTruncated` 事件让截断的工具调用可恢复重试，不再静默假装写完。dev 环境重编 + CDP 实测 glm-5-2 验证生效。⚠️验证中发现一个新坑（未修，留下一轮）：kimi-k3 超长请求会撞网关连接超时静默失败（与本次截断 bug 是不同机制）。详见 1oneUI 那份文档：
  [`../1oneUI/docs/guides/session-2026-07-21-truncated-tool-call-recovery.zh-CN.md`](../1oneUI/docs/guides/session-2026-07-21-truncated-tool-call-recovery.zh-CN.md)
- **【2026-07-20】三仓上游二次同步（本仓 v0.1.48→9 提交，`factory/aionrs.rs`/`manager/aionrs/agent.rs`/`dream-core-db` 均有改动）**：跟上游 #641 撤销了 fork 自建的"按模型最大输出 Token"功能分支，`max_tokens` 全链路恒为 `None`（⚠️ 设置页那个"最大输出"输入框现在是死 UI，填了不生效，待下一轮清理或重新设计）；`Cargo.toml` 的 `aion-*` 依赖仍钉 `gaogg521/aionrs` `master`（未改上游裸 tag，fork 铁律不变）；migration 撞号 025→030；顺带修了 `dream-core-db` 里两个已经红了 9 天的品牌断言 + 两处更早的迁移编号遗漏。详见 1oneUI 仓库这份文档（含 aionrs 侧的输出截断续写修复，是本次同步的直接起因）：
  [`../1oneUI/docs/guides/session-2026-07-20-truncation-fix-and-upstream-resync.zh-CN.md`](../1oneUI/docs/guides/session-2026-07-20-truncation-fix-and-upstream-resync.zh-CN.md)
- **【2026-07-20】企业组织 vs 项目组彻底解耦（新增 dream-domain-enterprise crate，本仓 dream-domain-org/dream-domain-sso/dream-core-app 均有改动；含三轮：架构重构+用户真机点出问题+全局测试）**：⚠️ **下面 07-16 条目描述的 `EnterpriseAutoJoiner`/`auto_provision_enterprise`/`one_tenants.sso_provider` 架构已被本次彻底取代**——SSO 公司维度独立到新 crate `dream-domain-enterprise`（`one_enterprises`/`one_enterprise_members`），`one_tenants` 回归纯邀请码项目组，trait 改名 `EnterpriseSync::sync_member`。真实开发库（非 `:memory:`）迁移冒烟已验证 `DROP COLUMN` 迁移可行；顺带修了 6 个 M4d（Bearer 豁免 CSRF）之后过期的测试断言（`active_lease_e2e.rs`/`agent_provider_health_e2e.rs`/`auth_e2e.rs`/`team_e2e.rs`）+ 一处品牌迁移021遗漏的测试断言。**`cargo test --workspace` 全绿仅发现 1 个未修的既有 Windows 平台 bug**：`dream-core-common::validate_workspace_path_availability`（`crates/dream-core-common/src/error.rs:242`）用 `fs::metadata()` 判断路径可用性，但 Win32 API 会静默剥掉路径分量末尾的空格/点号，导致带尾随空格的 workspace 路径校验在 Windows 上失效（Linux/macOS 正常）；07-18 上游同步 `3634e5b8` 带入，属 `dream-core-common` 基础层改动需评估影响面，留下一轮：
  [`../1oneUI/docs/guides/session-2026-07-20-enterprise-org-decouple.zh-CN.md`](../1oneUI/docs/guides/session-2026-07-20-enterprise-org-decouple.zh-CN.md)
- **【2026-07-16】飞书桌面登录根治 + 「真实企业」层(B2，本仓 dream-domain-sso/dream-domain-org/dream-core-app 均有改动，⚠️架构已被 07-20 取代，见上条)**：
  [`../1oneUI/docs/guides/session-2026-07-16-enterprise-tier-and-sso-fixes.zh-CN.md`](../1oneUI/docs/guides/session-2026-07-16-enterprise-tier-and-sso-fixes.zh-CN.md)
  - 一句话：桌面 SSO 登录走不完最后一步 = `dream-domain-sso` 的 `desktop_callback_page` 里 **`setTimeout(window.close, 1200)` 抢掉了浏览器的协议确认框**（与 scheme/平台无关），已改 5 秒；deep link 补传 `name`（服务器有「赵高」但只传了被 sanitize 成 `sso_xxx` 的 username）。
  - 新增「真实企业」层：`ProviderUserInfo.org_external_id`(飞书 `tenant_key`) → `create_tenant` 绑公司 → `OrgService::auto_provision_enterprise` 同公司 SSO 登录自动入伙；经 `dream_domain_sso::EnterpriseAutoJoiner` trait + `dream-core-app` 的 `OrgEnterpriseAutoJoiner` adapter 接线（同层 crate 只能靠 trait）。**callback 必须在 `issue_session` 之前调用**（auto-join 会轮换 jwt secret）。
  - **⚠️ 红线**：`auto_provision_enterprise` **join-only、永不建 tenant**（否则单机装机会被悄悄变成企业服务器），`auto_joiner` 为 `Option`；有专门测试 `auto_provision_enterprise_never_creates_a_tenant` 锁死。
  - 新增两个迁移账本条目：`dream-domain-org/004_tenant_sso_binding`、`dream-domain-sso/004_identity_org_external_id`。**改完必须 `backend-rebuild.ps1` 重编进 bundled 才生效**。
- **aionrs OpenAI 协议 thinking 参数 / 网关拒绝 tool_calls / 文本化工具历史兜底 / 授权模式默认全自动**（2026-07-10~11）：
  完整分析、三仓 commit 索引、黑盒探测网关方法论、上游对齐（issue #74 / PR #203）都在 1oneUI 仓库的这份 session 文档：
  [`../1oneUI/docs/guides/session-2026-07-10-thinking-param-and-rename.zh-CN.md`](../1oneUI/docs/guides/session-2026-07-10-thinking-param-and-rename.zh-CN.md)
  - 一句话：deepseek 等模型 Agent 任务报 `content[].thinking must be passed back` = 网关 `litellm-internal.123u.com` 的 DeepSeek 渠道**无状态拒绝一切 tool_calls 历史**（客户端无法用请求格式满足），aionrs fork 已用「文本化工具历史」绕过（commit `1f36350`）。
  - `session_mode` 默认 `yolo`（授权全自动）的后端兜底在 `dream-core-conversation/src/service.rs` 的 `resolve_assistant_snapshot`（仅对从未用过、`default_permission_mode==auto` 的 aionrs 助手生效）。
- **改完 Rust 必须重编** `aioncore.exe` 并搬进 1oneUI bundled 才生效（详见 `../1oneUI/docs/guides/ai-handoff-conventions.zh-CN.md`）。
